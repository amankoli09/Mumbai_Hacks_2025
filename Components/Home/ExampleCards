import React from "react";
import { motion } from "framer-motion";
import { Video, Image as ImageIcon, FileText, TrendingUp } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";

export default function ExampleCards({ onExampleClick }) {
  const examples = [
    {
      id: 1,
      title: "Recent Viral Fake News",
      description: "Alleged government policy announcement spreading on social media",
      type: "url",
      url: "https://twitter.com/example/status/123456789",
      icon: FileText,
      badge: "Viral",
      badgeColor: "bg-red-500 text-white",
      bgGradient: "from-amber-50 to-orange-50",
      iconBg: "bg-amber-500"
    },
    {
      id: 2,
      title: "Edited Video Claim",
      description: "Celebrity statement taken out of context",
      type: "url",
      url: "https://youtube.com/watch?v=example123",
      icon: Video,
      badge: "Trending",
      badgeColor: "bg-orange-500 text-white",
      bgGradient: "from-orange-50 to-red-50",
      iconBg: "bg-orange-500"
    },
    {
      id: 3,
      title: "AI-Generated Image",
      description: "Suspicious portrait circulating as real photograph",
      type: "text",
      text: "Breaking: Scientists discover cure for aging, available next month according to leaked documents.",
      icon: ImageIcon,
      badge: "New",
      badgeColor: "bg-blue-500 text-white",
      bgGradient: "from-blue-50 to-indigo-50",
      iconBg: "bg-blue-500"
    }
  ];

  return (
    <div className="mb-12">
      <div className="flex items-center gap-2 mb-4">
        <TrendingUp className="w-5 h-5 text-slate-600" />
        <h3 className="text-lg font-bold text-slate-900">Try These Examples</h3>
      </div>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {examples.map((example, index) => (
          <motion.div
            key={example.id}
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.3 + index * 0.1, duration: 0.4 }}
          >
            <Card 
              className={`cursor-pointer hover:shadow-xl hover:scale-105 transition-all duration-300 bg-gradient-to-br ${example.bgGradient} border-2 border-transparent hover:border-blue-300`}
              onClick={() => onExampleClick(example)}
            >
              <CardContent className="p-5">
                <div className="flex items-start gap-3 mb-3">
                  <div className={`w-12 h-12 ${example.iconBg} rounded-xl flex items-center justify-center flex-shrink-0 shadow-md`}>
                    <example.icon className="w-6 h-6 text-white" />
                  </div>
                  <div className="flex-1 min-w-0">
                    <div className="flex items-start justify-between gap-2 mb-2">
                      <h4 className="font-bold text-slate-900 text-sm line-clamp-1">
                        {example.title}
                      </h4>
                      <Badge className={`${example.badgeColor} text-xs flex-shrink-0 border-0`}>
                        {example.badge}
                      </Badge>
                    </div>
                    <p className="text-xs text-slate-600 line-clamp-2 mb-3">
                      {example.description}
                    </p>
                    <div className="text-xs text-blue-600 font-semibold flex items-center gap-1">
                      Click to verify →
                    </div>
                  </div>
                </div>
              </CardContent>
            </Card>
          </motion.div>
        ))}
      </div>
    </div>
  );
}