import React from "react";
import { motion } from "framer-motion";
import { Upload, Cpu, CheckCircle, Share2 } from "lucide-react";

export default function HowItWorks() {
  const steps = [
    {
      icon: Upload,
      title: "Paste/Upload",
      description: "Submit a URL, text, or image you want to verify",
      color: "from-blue-500 to-blue-600"
    },
    {
      icon: Cpu,
      title: "AI Analysis",
      description: "Our AI cross-checks with trusted fact-checking sources",
      color: "from-purple-500 to-purple-600"
    },
    {
      icon: CheckCircle,
      title: "Get Results",
      description: "Receive detailed credibility score and verdict in seconds",
      color: "from-emerald-500 to-emerald-600"
    },
    {
      icon: Share2,
      title: "Share Truth",
      description: "Spread verified information to combat misinformation",
      color: "from-amber-500 to-amber-600"
    }
  ];

  return (
    <div className="py-16 bg-gradient-to-br from-slate-50 to-blue-50 rounded-3xl px-8">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5 }}
        className="text-center mb-12"
      >
        <h2 className="text-3xl md:text-4xl font-bold text-slate-900 mb-3">
          How It Works
        </h2>
        <p className="text-lg text-slate-600 max-w-2xl mx-auto">
          Our AI-powered verification system analyzes content in 4 simple steps
        </p>
      </motion.div>

      <div className="grid grid-cols-1 md:grid-cols-4 gap-8">
        {steps.map((step, index) => (
          <motion.div
            key={step.title}
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.2 + index * 0.15, duration: 0.5 }}
            className="relative"
          >
            {index < steps.length - 1 && (
              <div className="hidden md:block absolute top-12 left-[60%] w-full h-0.5 bg-gradient-to-r from-blue-200 to-transparent z-0" />
            )}
            <div className="relative z-10 text-center">
              <div className={`w-20 h-20 bg-gradient-to-br ${step.color} rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-lg shadow-${step.color.split('-')[1]}-500/30`}>
                <step.icon className="w-10 h-10 text-white" />
              </div>
              <div className="bg-white rounded-xl p-5 shadow-md">
                <h3 className="font-bold text-lg text-slate-900 mb-2">
                  {step.title}
                </h3>
                <p className="text-sm text-slate-600">
                  {step.description}
                </p>
              </div>
            </div>
          </motion.div>
        ))}
      </div>
    </div>
  );
}